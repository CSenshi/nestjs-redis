# schedule

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build schedule` to build the library.

## Running unit tests

Run `nx test schedule` to execute the unit tests via [Jest](https://jestjs.io).
